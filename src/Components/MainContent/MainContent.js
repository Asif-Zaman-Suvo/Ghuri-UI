import React from 'react';

const MainContent = ({item}) => {
   

    
    return (
        <div>

            <h1>{item.products}</h1>
            
        </div>
    );
};

export default MainContent;